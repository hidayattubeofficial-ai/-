[build]
  publish = "."

# Keep the server-side backend out of the static site.
[[redirects]]
  from = "/backend/*"
  to = "/404.html"
  status = 404

# Messenger API is expected to be provided by a separate secure backend.
# Do not place API credentials in this static frontend.
